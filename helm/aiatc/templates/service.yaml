apiVersion: v1
kind: Service
metadata:
  name: {{ include "aiatc.fullname" . }}
  labels:
    {{- include "aiatc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aiatc.selectorLabels" . | nindent 4 }}
