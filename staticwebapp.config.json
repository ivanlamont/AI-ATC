{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/css/*", "/js/*", "/images/*", "/*.svg", "/*.png", "/*.ico", "/*.json", "/*.wasm", "/*.woff", "/*.woff2"]
  },
  "routes": [
    {
      "route": "/*",
      "rewrite": "/index.html"
    }
  ],
  "mimeTypes": {
    ".wasm": "application/wasm"
  }
}
